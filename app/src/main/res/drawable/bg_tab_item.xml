<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"  android:color="?attr/colorControlHighlight">

    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/common_8dp"/>
            <solid android:color="@color/mint"/>
            <stroke android:color="@color/mint" android:width="4dp"/>
        </shape>
    </item>

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/common_36dp" />
            <solid android:color="@color/white" /> <!-- Màu nhấn ripple -->
        </shape>
    </item>

</ripple>